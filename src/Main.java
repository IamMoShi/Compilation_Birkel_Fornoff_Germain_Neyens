import Grammar.AdaGrammar;


// Press Shift twice to open the Search Everywhere dialog and type `show whitespaces`,
// then press Enter. You can now see whitespace characters in your code.
public class Main {
    public static void main(String[] args) {
        // Obtenir le répertoire de travail courant
        // String repertoireCourant = System.getProperty("user.dir");


        AdaGrammar grammar = new AdaGrammar();
        grammar.grammar1("with Ada.Text_IO ; use Ada.Text_IO ; procedure unDebut is type maurice is access Leo ; ");
    }
}